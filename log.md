# HARP experiment log
# One line per closed experiment, key=value, semicolon-separated.
# The metric key (uppercased) below is whatever `targets[].primary_metric`
# is set to in harness.yaml (e.g. BEST_VAL_MAE, VAL_ACC, PPL).
# Format: TS=<iso>;PLAN=<id>;ANCHOR=<dir>;AXIS=<axis>;<PRIMARY_METRIC>=<float>;STATUS=<ok|below_expect|crash|unmapped>;GIT=<keep|discard|crash|N/A>;HP=<key=val,...>
